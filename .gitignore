*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
# Kotlin Gradle plugin scratch: compiler session markers and error logs, written
# on every build. One was committed by accident (removed alongside this line) —
# they are per-machine build noise and carry nothing a reader or a build needs.
.kotlin/

# signing secrets
/keystore.properties
*.keystore
app/google-services.json

# overnight hardening tooling (do not commit)
OVERNIGHT_PROMPT.md
run-overnight.sh
.overnight/
