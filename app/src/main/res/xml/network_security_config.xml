<?xml version="1.0" encoding="utf-8"?>
<!-- Release traffic is HTTPS-only (mirrors iOS ATS). Cleartext is permitted ONLY
     to the local dev backend (emulator host 10.0.2.2 / localhost) so a debug build
     can reach a dev server; production (pathway.nuruplace.org) stays TLS-only. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
